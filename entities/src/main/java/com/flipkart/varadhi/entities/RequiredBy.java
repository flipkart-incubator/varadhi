package com.flipkart.varadhi.entities;

/**
 * Indicates where a standard header is required: Subscription-only, Queue-only, or both.
 */
public enum RequiredBy {
    Subscription,
    Queue,
    Both
}
